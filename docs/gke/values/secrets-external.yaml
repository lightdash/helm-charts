# The provider must create these Secrets before Helm runs.
secrets: null
existingSecret: ""

secretRefs:
  enabled: true
  application:
    name: lightdash-application
  database:
    name: lightdash-database
    passwordKey: PGPASSWORD
  s3:
    name: lightdash-s3
    accessKey: S3_ACCESS_KEY
    secretKey: S3_SECRET_KEY
  migration:
    name: lightdash-application

externalDatabase:
  password: ""
  existingSecret: ""

s3:
  accessKey: ""
  secretKey: ""
  existingSecret: ""
